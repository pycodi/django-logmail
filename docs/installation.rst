============
Installation
============

At the command line::

    $ easy_install django-logmail

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-logmail
    $ pip install django-logmail
